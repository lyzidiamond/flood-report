// MapLibre implementation — handled separately
// This component receives an onMapClick(lat, lng) prop from App.jsx

export default function Map({ onMapClick }) {
  // TODO: implement MapLibre map
  // - full-screen map of the US
  // - call onMapClick(lat, lng) on map click
  return null;
}
